// DemoBanner intentionally left minimal — we use inline indicators on the analysis page
// rather than a persistent global banner that clutters every screen.
export default function DemoBanner() { return null }
